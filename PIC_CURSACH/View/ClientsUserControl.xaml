<UserControl x:Class="PIC_CURSACH.View.ClientsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PIC_CURSACH.View"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             Background="{DynamicResource ApplicationBackgroundBrush}"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid Margin="10">
        <StackPanel>
            <ui:TextBlock Text="Модуль Клиенты" FontSize="24" FontWeight="Bold"
                          Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>

            <DataGrid ItemsSource="{Binding Clients}" AutoGenerateColumns="False"
                      SelectedItem="{Binding SelectedClient}"
                      CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding ClientId}" IsReadOnly="True" Width="60" />
                    <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="*" />
                    <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="*" />
                    <DataGridTextColumn Header="Паспорт" Binding="{Binding Passport}" Width="*" />
                    <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="*" />
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                <ui:Button Content="Добавить" Command="{Binding AddClientCommand}" Margin="5" Width="100" />
                <ui:Button Content="Редактировать" Command="{Binding EditClientCommand}" Margin="5" Width="150" />
                <ui:Button Content="Удалить" Command="{Binding DeleteClientCommand}" Margin="5" Width="100" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
